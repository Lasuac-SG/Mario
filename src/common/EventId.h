//

#ifndef MARIO_EVENTID_H
#define MARIO_EVENTID_H
enum class MarioState { IDLE, RUNNING, JUMPING, FALLING };
enum class TileType { EMPTY = 0, GROUND = 1, PIPE = 2 };
enum class Direction { LEFT, RIGHT };

#endif  // MARIO_EVENTID_H
